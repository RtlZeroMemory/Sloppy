{
  "schemaVersion": 1,
  "matrix": "sloppy.release-install-verification",
  "statusVocabulary": ["PASS", "FAIL", "SKIPPED", "UNAVAILABLE", "DEFERRED", "NOT RUN"],
  "lanes": [
    {
      "id": "windows-archive",
      "status": "NOT RUN",
      "requiredEvidence": ["package", "test-package", "test-install", "sloppy create", "sloppy build", "sloppy package", "sloppy --version", "sloppy doctor", "sloppyc --version", "prebuilt artifact smoke"],
      "notes": "Filled by local/CI evidence in the release-dist PR body."
    },
    {
      "id": "linux-archive",
      "status": "NOT RUN",
      "requiredEvidence": ["Linux source build", "Linux package", "outside-checkout smoke", "test-install", "sloppy create", "external app build", "runtime execution or explicit V8 blocker"],
      "notes": "Linux x64 is required. Fill package/source smoke evidence in the PR body. V8 runtime-user support requires the Sloppy-owned Linux x64 SDK plus require-v8 package smoke."
    },
    {
      "id": "macos-arm64-archive",
      "status": "NOT RUN",
      "requiredEvidence": ["hosted package dry-run", "outside-checkout smoke"],
      "notes": "Hosted evidence belongs to the manual release-artifacts workflow."
    },
    {
      "id": "macos-x64-archive",
      "status": "NOT RUN",
      "requiredEvidence": ["hosted x64 package dry-run", "npm pack dry-run"],
      "notes": "Required when publishing the macOS x64 npm platform package."
    },
    {
      "id": "npm-root-launcher",
      "status": "NOT RUN",
      "requiredEvidence": ["npm pack dry-run", "local tarball install", "sloppy --version", "sloppy doctor", "sloppy create", "sloppy build", "missing platform diagnostic", "unsupported platform diagnostic"],
      "notes": "npm is a runtime installer only."
    },
    {
      "id": "npm-platform-packages",
      "status": "NOT RUN",
      "requiredEvidence": ["platform package generated from archive contents", "npm pack dry-run", "no native install scripts", "alpha dist-tag policy"],
      "notes": "Windows x64, Linux x64 GNU, macOS arm64, and macOS x64 are the current npm platform package set for this alpha. Platform packages must not independently build Sloppy."
    },
    {
      "id": "v8-package-lane",
      "status": "PASS",
      "requiredEvidence": ["V8 SDK build or resolved hosted SDK", "V8-enabled build", "V8-backed artifact execution", "V8-backed source-input execution from extracted package"],
      "notes": "Local Docker evidence passed for Linux x64 in sloppy-linux-v8-runtime-final-808584149 on May 8, 2026. Non-V8 package smoke is not V8 runtime evidence. Static Linux V8 packages may link V8 into bin/sloppy instead of bundling separate runtime files."
    },
    {
      "id": "live-providers",
      "status": "SKIPPED",
      "requiredEvidence": ["PostgreSQL live lane", "SQL Server live lane"],
      "notes": "Live providers are opt-in and not default package evidence."
    }
  ]
}
