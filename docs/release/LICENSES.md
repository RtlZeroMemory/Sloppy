# License and Notice Policy

Sloppy is licensed under the Apache License, Version 2.0. Every package must
include the project `LICENSE` file at its root.

A complete third-party license review is required before publishing. That
review must inventory bundled native runtime libraries, optional V8 runtime
files, compiler dependencies, and generated notices required by upstream
dependencies.

Package scripts must not include source-only dependency caches, V8 SDK headers,
V8 import libraries, vcpkg source trees, or build directories.
