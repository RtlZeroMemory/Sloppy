# Fuzz Tests

Fuzz tests will cover parsers and untrusted input boundaries, including future plan JSON,
HTTP/config parsing, and compiler extraction inputs.

libFuzzer-style fuzzing is planned where toolchains support it.
