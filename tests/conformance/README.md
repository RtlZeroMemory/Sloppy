# End-to-End Conformance Suite

Status: MAIN1-13 executable conformance layout.

Evidence lanes and naming policy are defined by
`docs/project/engine-19-conformance-matrix.md`. This README describes current cases; the
matrix owns default non-V8, V8-gated, localhost transport, SQLite/capability, package
outside-checkout, live-provider optional, stress/smoke, and benchmark harness reporting
rules.

This suite protects the alpha-supported workflow through the real toolchain:

```powershell
sloppyc build <source.js> --out <artifacts>
sloppy run --artifacts <artifacts> --once GET /
```

Default non-V8 tests compile supported sources, verify deterministic artifact sets, and
exercise unsupported compiler/runtime failure paths. V8-gated tests compile and execute
supported artifacts through `sloppy run --artifacts --once` when the build enables V8.

Conformance cases:

| Area | Source fixture | Default evidence | V8-gated evidence | Deferred or gated requirements |
| --- | --- | --- | --- | --- |
| Hello | `examples/compiler-hello/app.js` | `conformance.hello.compile_artifacts` verifies deterministic `app.plan.json`, `app.js`, and `app.js.map`. | `conformance.hello.run_once` compiles and runs `GET /`, expecting `Hello from Sloppy`. | V8 SDK required for execution. |
| Request context | `examples/request-context/app.js` | `conformance.request_context.compile_artifacts` verifies deterministic artifacts. | `conformance.request_context.run_once` checks route params, repeated query last-wins, method, path, and raw target. | Header/body context is covered by HTTP unit/dispatch and V8-gated integration tests until socket-mode conformance can send body-bearing requests. |
| ENGINE-02/04 compiler metadata | `compiler/tests/fixtures/http-methods/input.js`, `async-handler/input.js`, `provider-capability/input.js`, `source-map/input.js` | Compile-artifact tests verify deterministic method metadata, async metadata, provider/capability plan metadata, and source-map artifacts. | `conformance.async_handler.run_once` executes the direct async handler fixture when V8 is enabled. HTTP dispatch integration covers non-GET handler execution when V8 is enabled. | Broader async handler source shapes and source-map remapping remain later ENGINE layers. |
| HTTP | `tests/conformance/http/README.md` | `conformance.http.default_dispatch` protects synthetic GET/POST/PUT/PATCH/DELETE dispatch, route miss, method mismatch, route params, malformed query/JSON, unsupported media/body framing, and body limits. `conformance.transport.localhost_mvp`, `conformance.transport.keep_alive`, `conformance.transport.keep_alive_idle_timeout`, `conformance.transport.keep_alive_max_requests`, `conformance.transport.lifecycle_reset`, `conformance.transport.chunked_request`, `conformance.transport.streaming_response`, `conformance.transport.backpressure`, `conformance.transport.shutdown_cancel`, and `smoke.transport.keep_alive_streaming_bounded` protect bounded localhost keep-alive/chunked/streaming conformance and stress smoke. | `conformance.v8.http_dispatch_execution` proves synthetic HTTP dispatch through V8 handlers, including non-GET methods, headers, and JSON body materialization. | Localhost proof is bounded loopback transport evidence only, not production-edge HTTP, TLS, HTTP/2/3, WebSockets, SSE, multipart, compression, static files, public streaming APIs, benchmark, or V8 evidence. |
| Async | `tests/conformance/async/README.md` | `conformance.async.native_settlement`, `conformance.async.backend_completion`, and `conformance.async.backend_libuv` protect native settlement, bounded completion transport, cleanup-once ownership, overflow, and owner-thread backend dispatch. | `conformance.v8.native_async_scheduler`, `conformance.v8.runtime_bridge`, and `conformance.async_handler.run_once` prove V8 Promise settlement, rejection, pending/deadline behavior, and native continuation scheduling only when V8 is configured. | Default async tests do not prove V8 Promise behavior; V8-gated success is not default success. |
| V8 runtime | `tests/conformance/v8/README.md` | Default gates cover only V8-disabled/noop failure behavior and static bootstrap assets. | `conformance.v8.runtime_bridge`, `conformance.v8.owner_thread`, `conformance.v8.native_async_scheduler`, `conformance.v8.http_dispatch_execution`, and `conformance.*.run_once` make the V8-gated runtime lane visible. | Missing SDK means skipped/not configured, not passed. This is not package, live-provider, benchmark, or public alpha evidence. |
| Time | `tests/conformance/time/README.md` | `core.diagnostics.foundation`, `examples.time.api_shape`, and `bootstrap.stdlib.app_host_foundation` cover stable Time diagnostics, source examples, deterministic fake-clock behavior, intervals, scheduled jobs, cancellation/deadline validation, and filesystem Time option behavior. | `conformance.v8.runtime_bridge` covers native delay Promise settlement through the owner-thread scheduler and inactive `__sloppy.time` registration when V8 is configured. | No Node timer compatibility, global fake timers, cron parser, benchmark, package-manager, or unrelated network/crypto/process claims. |
| Results | `examples/compiler-hello/app.js`, `examples/request-context/app.js`, and `tests/integration/execution/invalid_descriptor/` | Compiler and run-path negative fixtures remain default/non-V8 where startup does not need V8. | Hello covers `Results.text`; request-context covers `Results.json`; V8 integration covers custom result headers; `conformance.results.invalid_descriptor` expects safe `500`. | Streaming, files, redirects, and content negotiation remain deferred. |
| Unsupported behavior | `compiler/tests/fixtures/unsupported-*`, `tests/fixtures/run/*` | Dynamic route, bare import, unsupported async handler body, and secret-bearing capability conformance reject before artifact success; existing `sloppy.run.*` tests cover source-input deferral, missing/malformed artifacts, V8-disabled diagnostics, and plan drift. | Unsupported method/result cases return safe dev responses when V8 is enabled. HTTP dispatch tests cover malformed JSON, oversized bodies, unsupported content types, and unsupported transfer framing. | Socket-mode body conformance remains future work. |
| Capability | `tests/conformance/capability/README.md` | `conformance.capability.native_registry` covers allowed/denied database policy, provider mismatch, missing capability, wrong kind, filesystem/network metadata checks, and redacted diagnostics. `conformance.capability.provider_executor` covers capability denial before queue admission, ownership transfer, provider work, or cleanup. | V8 SQLite bridge tests cover allowed use plus denied open/read/write/provider cases before SQLite provider work; `conformance.users_api_sqlite.localhost_transport` also verifies denied capability over loopback TCP. | PostgreSQL and SQL Server JS bridges remain deferred. Filesystem/network APIs remain metadata/check-only skeletons. |
| SQLite | `tests/conformance/sqlite/README.md` | `conformance.sqlite.native_provider` covers `:memory:` open/close, exec, query, `queryOne`, null/int/float/text/blob parameters and results, empty sets, invalid SQL, unsupported parameters, diagnostics redaction, transaction lifecycle, and cleanup. | `conformance.sqlite.bridge` runs the V8-gated `:memory:` SQLite JS bridge with a declared capability and expects the selected row. `conformance.sqlite.denied_capability` and `conformance.users_api_sqlite.localhost_transport` cover denied capability and users API behavior. | PostgreSQL and SQL Server JS bridges remain deferred. Users API transport proof is localhost evidence, not production-edge HTTP. |

These tests are not benchmark evidence, live provider evidence, production HTTP evidence,
package release evidence, or Node/npm compatibility evidence.
