{
  "contract": "Dynamic route inputs can build with partial Plan metadata while static routes remain runnable.",
  "routes": [
    {
      "method": "GET",
      "path": "/health",
      "handler": "Health.Get"
    }
  ],
  "sourceMap": "present"
}
