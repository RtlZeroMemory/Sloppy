module.exports.sticky = "kept";
// `exports = ...` rebinds the local `exports` parameter, not `module.exports`.
// Node semantics: this is a no-op for consumers and `module.exports` retains
// the keys set above. The fixture proves Sloppy matches that behavior.
exports = { rebound: "ignored" };
