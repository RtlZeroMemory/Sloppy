const fs = require("node:fs");
// Asset is intentionally not declared in `files`; sealed sync read must reject.
module.exports = function read() {
    return fs.readFileSync(__dirname + "/missing.txt", "utf8");
};
