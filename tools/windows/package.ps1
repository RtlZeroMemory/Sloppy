param(
    [string]$Configuration = "windows-release",
    [string]$OutputDir = "artifacts"
)

$ErrorActionPreference = "Stop"

Write-Host "Release packaging is not implemented yet."
Write-Host "Future package target: GitHub Release ZIP first."
Write-Host "Requested configuration: $Configuration"
Write-Host "Requested output directory: $OutputDir"
