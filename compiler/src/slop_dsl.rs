//! Slop DSL recognition helpers.
//!
//! Future slices move explicit `Sloppy`, route, provider, schema, config, and `Results`
//! call matching here instead of treating arbitrary JavaScript as compiler truth.
