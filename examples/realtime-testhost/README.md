# Realtime TestHost

Minimal TestHost-only realtime example. It demonstrates the typed
`send(...)`/`expect(...)` helper pair without external WebSocket packages.

```powershell
sloppy run examples/realtime-testhost/test.mjs
```
