# Workers Background Service

This example shows a lifecycle-bound background service shape.

Shows a lifecycle-bound `BackgroundService` registered with `app.use(...)`.

The service uses a cancellation-aware loop and avoids logging payload data.
