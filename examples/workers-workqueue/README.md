# Workers WorkQueue

Shows a bounded in-process FIFO queue with explicit capacity, concurrency, overflow, and
retry policy.

Queue diagnostics and doctor output report policy metadata only; job payload values are not
included in diagnostics or goldens.
