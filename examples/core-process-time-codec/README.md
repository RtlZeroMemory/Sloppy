# Core Process Time Codec Example

This example reads process output
as owned bytes or text, applies a shared Deadline, and decodes bytes with Codec UTF-8.

The example documents implemented API shape only. It has no public release claim, no
package-manager behavior, no Node/Bun/Deno compatibility promise, no production-readiness
claim, and no benchmark or performance claim.
