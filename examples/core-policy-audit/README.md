# Core Policy Audit Example

This example keeps capability and
strict-policy metadata explicit so doctor/audit output can describe implemented and deferred
policy behavior honestly.

The policy object is example metadata, not an OS sandbox or package-manager manifest. The
example documents implemented API shape only. It has no public release claim, no
Node/Bun/Deno compatibility promise, no production-readiness claim, and no benchmark or
performance claim.
