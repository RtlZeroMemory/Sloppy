# Jobs SQL Server Worker

Shows a SQL Server-backed scheduler worker run through Sloppy Program Mode.
Set `SLOPPY_JOBS_SQLSERVER_CONNECTION_STRING` before running it.

```sh
sloppy run examples/jobs-sqlserver-worker/main.ts
```
