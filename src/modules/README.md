# Modules

This directory is reserved for future runtime module graph support.

No module runtime is implemented in the foundation phase. See
`docs/api/app.md` for the public module API and
`docs/internals/architecture.md` for the architecture context.
