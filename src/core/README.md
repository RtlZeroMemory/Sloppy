# Runtime Core

The C runtime core will own lifecycle, memory, diagnostics, configuration, permissions,
resources, plan loading, native route dispatch, and host services.

No runtime feature code exists here yet. Add modules only after their ownership, tests,
diagnostics, and public headers are specified.
