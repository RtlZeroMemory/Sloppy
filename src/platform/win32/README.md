# Win32 Platform Backend

Windows-specific platform implementations belong here.

Allowed here only:

- WinAPI includes;
- WinAPI calls;
- Windows-specific dynamic library behavior such as `LoadLibrary` and `GetProcAddress`.

No implementation exists in the foundation phase.
