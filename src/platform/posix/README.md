# POSIX Platform Backend

POSIX-generic platform implementations belong here.

Allowed here only:

- POSIX includes;
- POSIX calls;
- POSIX dynamic library behavior such as `dlopen` and `dlsym`.

No implementation exists in the foundation phase.
