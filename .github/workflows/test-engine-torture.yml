name: test-engine-torture

on:
  workflow_dispatch:
    inputs:
      area:
        description: "Test engine area to run."
        required: true
        default: "all"
        type: choice
        options:
          - all
          - fuzz
          - stress
          - sanitizer
          - http2
          - v8
          - provider
      seed:
        description: "Seed for randomized/property lanes."
        required: true
        default: "12345"
        type: string
      fuzz_iterations:
        description: "Fuzz/property iterations per target."
        required: true
        default: "120000"
        type: string
      stress_seconds:
        description: "Default stress budget in seconds."
        required: true
        default: "300"
        type: string

env:
  VCPKG_COMMIT: 031f4afe03be385aee354f15be6e6b1fe57497c7

jobs:
  windows:
    name: torture / windows
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Ensure Ninja
        shell: pwsh
        run: |
          if (-not (Get-Command ninja -ErrorAction SilentlyContinue)) {
            choco install ninja -y --no-progress
          }

      - name: Restore vcpkg cache
        id: vcpkg-cache
        uses: actions/cache/restore@v4
        with:
          path: |
            .sdeps/vcpkg
            .sdeps/vcpkg-bincache
            vcpkg_installed
          key: test-engine-torture-windows-vcpkg-${{ env.VCPKG_COMMIT }}-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json') }}
          restore-keys: |
            test-engine-torture-windows-vcpkg-${{ env.VCPKG_COMMIT }}-
            windows-vcpkg-${{ env.VCPKG_COMMIT }}-
            windows-vcpkg-

      - name: Bootstrap vcpkg
        shell: pwsh
        run: |
          $vcpkgRoot = Join-Path $PWD ".sdeps/vcpkg"
          $vcpkgBinaryCache = Join-Path $PWD ".sdeps/vcpkg-bincache"
          New-Item -ItemType Directory -Force -Path $vcpkgBinaryCache | Out-Null
          if (-not (Test-Path -LiteralPath $vcpkgRoot)) {
            New-Item -ItemType Directory -Force -Path (Split-Path -Parent $vcpkgRoot) | Out-Null
            git clone https://github.com/microsoft/vcpkg.git $vcpkgRoot
          }
          git -C $vcpkgRoot fetch origin
          git -C $vcpkgRoot checkout $env:VCPKG_COMMIT
          if (-not (Test-Path -LiteralPath (Join-Path $vcpkgRoot "vcpkg.exe"))) {
            & (Join-Path $vcpkgRoot "bootstrap-vcpkg.bat") -disableMetrics
          }
          "VCPKG_ROOT=$vcpkgRoot" | Out-File -FilePath $env:GITHUB_ENV -Append
          "VCPKG_DEFAULT_BINARY_CACHE=$vcpkgBinaryCache" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Toolchain bootstrap
        shell: pwsh
        run: .\tools\windows\bootstrap.ps1

      - name: Configure default preset
        shell: pwsh
        run: .\tools\windows\dev.ps1 configure -Preset windows-dev

      - name: Save vcpkg cache after configure
        if: steps.vcpkg-cache.outputs.cache-hit != 'true'
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: |
            .sdeps/vcpkg
            .sdeps/vcpkg-bincache
            vcpkg_installed
          key: test-engine-torture-windows-vcpkg-${{ env.VCPKG_COMMIT }}-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json') }}

      - name: Build default preset
        shell: pwsh
        run: .\tools\windows\dev.ps1 build -Preset windows-dev

      - name: Run torture test engine
        shell: pwsh
        run: |
          .\tools\windows\test-engine.ps1 `
            -Tier torture `
            -Area "${{ github.event.inputs.area }}" `
            -Seed ([int]"${{ github.event.inputs.seed }}") `
            -FuzzIterations ([int]"${{ github.event.inputs.fuzz_iterations }}") `
            -StressSeconds ([int]"${{ github.event.inputs.stress_seconds }}") `
            -Out artifacts\test-engine\torture-windows.json

      - name: Upload report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-engine-torture-windows
          path: artifacts/test-engine/torture-windows.json
          if-no-files-found: ignore
