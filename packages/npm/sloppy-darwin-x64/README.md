# @rtlzeromemory/sloppy-darwin-x64

This optional platform package is populated during release dry-run from
`sloppy-macos-x64.tar.gz` only when the optional macOS x64 lane is built and smoked.
It is not referenced by the root npm package or npm publish workflow in this PR.
