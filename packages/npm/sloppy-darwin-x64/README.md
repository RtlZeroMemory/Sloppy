# @rtlzeromemory/sloppy-darwin-x64

This optional platform package is populated during release dry-run from
`sloppy-macos-x64.tar.gz` when the macOS x64 lane is built and smoked.
It is referenced by the root npm launcher package for macOS x64 installs.
