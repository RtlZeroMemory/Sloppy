# @rtlzeromemory/sloppy-darwin-arm64

This platform package is populated during release dry-run from `sloppy-macos-arm64.tar.gz`.
It is the macOS arm64 alpha platform package for the Sloppy runtime and must
not build Sloppy independently.
